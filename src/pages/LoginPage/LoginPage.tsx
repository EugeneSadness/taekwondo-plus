import PageContainer, { SectionTitle } from '@/components/UI/Container/Container';

const LoginPage = () => {
  return (
    <PageContainer>
      <SectionTitle>Вход</SectionTitle>
      <p>Страница в разработке</p>
    </PageContainer>
  );
};

export default LoginPage;